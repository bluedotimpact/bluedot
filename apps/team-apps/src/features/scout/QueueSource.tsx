export const QueueSource = ({ count }: { count: number }) => (
  <p className="text-size-xs text-secondary">
    {`${count} people, from the Talent scouting view in Airtable, in its order. Who appears is decided there, not here.`}
  </p>
);
